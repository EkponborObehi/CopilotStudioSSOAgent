// Configuration file for SSO Agent
const CONFIG = {
  clientId: window.ENV?.CLIENTID,
  tenantId: window.ENV?.TENANTID,
  tokenEndpoint: window.ENV?.TOKENENDPOINT,
  customScope: window.ENV?.CUSTOMSCOPE,
};